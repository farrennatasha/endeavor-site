Place Taiata image here

Save the image attachment you provided as `taiata.jpg` inside this `images/` folder so the page will load it locally.

Recommended filename: `images/taiata.jpg`

If you prefer a different format, update the `src` in `endeavour_crew_1.html` accordingly.
